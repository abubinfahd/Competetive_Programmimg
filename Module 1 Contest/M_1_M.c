#include<stdio.h>
#include<math.h>
int main(){
    int n, m;
    int x, y;
    scanf("%d%d%d%d", &x, &y, &n, &m);
    int result = (x*n) + (y*m);
    printf("%d\n", result);


    return 0;

}
